@extends('layout.home-layout')

@section('content')
  @foreach($paganti as $pagante)

  <div class="box-container">
      <div class="box">{{  }}</div>
      <div class="box">{{  }}</div>
      <div class="box">{{  }}</div>
      <div class="box">{{  }}</div>
  </div>
  @endforeach
@stop
